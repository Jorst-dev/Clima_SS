# Primera parte
Este proyecto es una aplicación web simple creada con Python (Flask) y HTML + CSS que permite consultar el clima de cualquier ciudad utilizando la API de OpenWeatherMap.

#Tecnologías usadas
-Python
-Flask (para el servidor web)
-HTML5
-OpenWeatherMap API (para obtener la información del clima)
